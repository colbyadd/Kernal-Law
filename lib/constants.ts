/** Canonical base URL — single source of truth for sitemap, robots, JSON‑LD, and OG metadata. */
export const BASE_URL = 'https://kernallaw.com'

/** Canonical host policy: always serve apex domain (no www). */
export const CANONICAL_HOST = 'kernallaw.com'

/** Default "last modified" date used in sitemap entries when page-level dates are not tracked yet. */
export const DEFAULT_LAST_MODIFIED = '2026-02-17'
